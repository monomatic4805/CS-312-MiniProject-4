<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit Post</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <div class="create-container">
    <h1>Edit Post</h1>
    <form method="POST" action="/blogs/edit/<%= blog.blog_id %>">
      <input name="title" value="<%= blog.title %>" required />
      <textarea name="body" rows="8" required><%= blog.body %></textarea>
      <button type="submit">Save Changes</button>
    </form>
    <p><a href="/blogs" class="btn" style="margin-top:20px; display:inline-block;">Back to Feed</a></p>
  </div>
</body>
</html>
